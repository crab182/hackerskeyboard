# diyRAG gpu-runtime — pinned runtime dependencies (MASTER_BUILD_SPEC.md §3, §16, §19).
#
# This file mirrors the [project.dependencies] in pyproject.toml for environments
# that install with plain `pip install -r requirements.txt` (e.g. the Dockerfile).
# pyproject.toml is the source of truth; keep the two in sync. Versions are PINNED
# (§19). vLLM and OCR extras are split into the requirements-*.txt files below so
# the base image stays lean and only the active profile pulls heavy deps (§17).
#
# CUDA NOTE (§16): torch 2.5.1 expects the CUDA 12.1 runtime. Inside the CUDA base
# image install the matching wheel:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# On CPU-only nodes the Rust-native backend is used instead and this service is
# not started (§24.1); a CPU torch wheel is acceptable for tests.

# --- HTTP / serving layer ---------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-json-logger==3.2.1

# --- gRPC (Rust ingestion-worker / core-api call over gRPC) ----------------
grpcio==1.68.1
grpcio-tools==1.68.1
protobuf==5.29.2

# --- Deep-learning core (install torch from the CUDA wheel index — see note) -
torch==2.5.1
transformers==4.47.1
tokenizers==0.21.0

# --- Embeddings + reranker (BGE-M3 dense+sparse, bge-reranker-v2-m3) --------
FlagEmbedding==1.3.3
sentence-transformers==3.3.1

# --- Observability (§13.1) --------------------------------------------------
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
